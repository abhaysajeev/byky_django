Django==5.2.5
Pillow==11.0.0
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
# Direct, not transitive: apps/portal/jwt.py uses PyJWT directly, since
# djangorestframework-simplejwt's own Token classes import
# django.contrib.auth.models, which this project deliberately does not
# install (config/settings.py) -- see apps/portal/jwt.py's docstring.
PyJWT==2.14.0
psycopg[binary]==3.2.3
python-dotenv==1.1.1
whitenoise==6.9.0
gunicorn==23.0.0
drf-spectacular==0.30.0
# Ships the Swagger UI/Redoc static assets as package data, served by
# WhiteNoise like every other static file -- the Swagger page then loads
# with no jsdelivr/unpkg CDN call (config/settings.py SPECTACULAR_SETTINGS).
drf-spectacular-sidecar==2026.9.1
