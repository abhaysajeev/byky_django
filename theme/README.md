# theme

The ported UI shell: base layout, sidebar, partials and the template helpers the
screens rely on.

Nothing is here yet. The shell arrives with the first screen port, following
`.claude/skills/port-ui/SKILL.md`. Source of truth for the markup is the
wireframe project at
`source/ui-template/django-version/full-version/`, which is read-only.
